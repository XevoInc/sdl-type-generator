{
  "name": "sdl-type-generator",
  "version": "0.1.1",
  "description": "TypeScript d.ts file generator from SDL's HMI_API.xml",
  "scripts": {
    "prebuild": "tslint --project ./",
    "build": "tsc -p ."
  },
  "author": "Xevo Inc.",
  "license": "MIT",
  "main": "./index.js",
  "bin": {
    "sdl-type-generator": "./bin/sdl-type-generator"
  },
  "dependencies": {
    "xml2js": "^0.5.0"
  },
  "devDependencies": {
    "@types/xml2js": "^0.4.0",
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  }
}
